apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}"
  labels:
    microservice: "{{ .Release.Name }}"
data:
  PORT: "{{ .Values.port }}"
  PRODUCTS_API_BASE_URL: "{{ .Values.productsApiBaseUrl }}"
